{
  "name": "op-music-pree",
  "version": "1.0.0",
  "description": "OP Music Pree — API dependencies for Vercel serverless functions",
  "dependencies": {
    "axios": "^1.7.9"
  },
  "scripts": {
    "build": "rm -rf dist && cd client && npm install --legacy-peer-deps && npm run build && cd .. && cp -r client/dist dist"
  }
}
